# 🏗️ Architecture

The application is organized into `core`, `interface`, and `simulation` modules. The firewall acts as a TCP proxy between clients and a backend service, while the dashboard communicates with a local admin control socket.
